// TODO

export function convertPxToM(px: number, coord: "x" | "y"): number {
  return px;
}

export function convertMToPx(m: number, coord: "x" | "y"): number {
  return m
}

export function getHeadX(width: number): number {
  return width;
}

export function getCenterX(width: number): number {
  return width;
}

export function getHeadY(height: number): number {
  return height;
}

export function getCenterY(height: number): number {
  return height;
}